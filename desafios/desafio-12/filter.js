const numeros = [ 1, 8, 3, 12, 4, 7, 2, 9] // array de numeros
const numMaiors = numeros.filter( num => num > 5)  // filtra  somente os número maiors que 5 em uma nova array
console.log(numMaiors) // mostra os números filtrados