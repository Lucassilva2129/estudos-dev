const numeros = [2, 4, 6, 8, 10] // Array.
const numero = numeros.map(num => num *3) // Multiplica (transforma) cada item da array por 3.
console.log(numero) // Mostra o resultado dos números (transformados). 